s = [1,1,2,3,2,4];
t = [2,3,3,4,5,5];
w = [3,3,1,3,3,2];
names={'o','a','b','c','d'};
G = digraph(s,t,w,names);

% plot(G);

V = [1;2;3;4;5];
[U,V,C]=compute_cuts(V, G);

minC = min(C);
vecIndex = find(C==minC);
for i =1:size(vecIndex)
    fprintf('Minimum cut value: %d', minC);
    fprintf('U: ')
end
